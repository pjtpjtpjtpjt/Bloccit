<h1>All Posts</h1>
 <% @posts.each do |post| %>
  <div class="media">
     <div class="media-body">
       <h4 class="media-heading">
           <% if post.id % 5 == 0 then post.title = "CENSORED" end%>
         <%= link_to post.title, post %>
       </h4>
     </div>
   </div>
 <% end %>
